name: Update README

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 */1 * * *"
  workflow_dispatch:

jobs:
  markscribe:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}

      - uses: cboone/gh-actions/actions/run-markscribe@844bf31d4ec26df9d0aa531e79d8c3fccf9f0638 # v3.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
        with:
          template: templates/README.md.tpl
          write-to: README.md

      - name: Commit and push
        run: |
          git config user.name "Christopher Boone"
          git config user.email "cboone@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "docs: update generated README"
          git push
